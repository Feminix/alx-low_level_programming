#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */

int main(void)
{
	char c;
	
	c = 'a';
	k = 0;
	while
		(k < 10) {
			putchar(k + '0');
			k++;
		}
	while
		(c <= 'f') {
			putchar(c);
			c++
		}
	putchar('\n');
	return (0);
}
